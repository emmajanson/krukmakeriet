import "@testing-library/jest-dom";

describe("LogIn", () => {
  test("check if user can log in", async () => {
  });

  test("check what happens if wrong credentials", async () => {
  });

  test("does user get redirected", async () => {
  });

  test("are forms correctly filled out", async () => {
  });

});
