import "@testing-library/jest-dom";

describe("Profile", () => {
  test("rendere data of account info from fetch?", async () => {
  });

  test("rendere data of booked courses from fetch?", async () => {
  });
  test("rendere data of purchased items from fetch?", async () => {
  });

  test("logout btn", async () => {
  });

});
