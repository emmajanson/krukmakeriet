import "@testing-library/jest-dom";

describe("Checkout", () => {
  test("snapshoty of structure", async () => {
  });

  test("right elements are rendered", async () => {
  });

  test("on click a validation message", async () => {
  });
});
